import React from 'react';
import withLayout from '../hocs/withLayout';

const About = () => {
    return (
        <main>
            {/* Your About component content */}
        </main>
    );
};

export default withLayout(AppLayout)(About);
